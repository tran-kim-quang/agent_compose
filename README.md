# Compose agent for fintech project

## Install for dev

* Pull lastest version of repo and install all new dependency ```poetry install```

* To add dependency: ```poetry add [dependency-name]```

* To run a file: ```poetry run python file_name.py```